<p><%=t '.title'%> <%= @email %>!</p>

<% if @resource.try(:unconfirmed_email?) %>
  <p><%=t '.body'%> <%= @resource.unconfirmed_email %>.</p>
<% else %>
  <p><%=t '.body'%> <%= @resource.email %>.</p>
<% end %>
