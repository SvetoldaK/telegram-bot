<%= link_to t('.back'), user_chat_path(current_user.id, @chat.id) %>
<%= link_to t('.delete'), user_chat_group_path(current_user.id, @chat.id, @group.id),
  method: :delete, data: { turbo_method: 'delete', turbo_confirm: t('.sure')}%> 
<%= link_to t('.edit'), edit_user_chat_group_path(current_user.id, @chat.id, @group.id) %>
<p><%= t('.command')%> <%= ": #{@group.name}" %></p>
<p><%= t('.nickname')%> <%= ": #{@group.nickname}" %></p>